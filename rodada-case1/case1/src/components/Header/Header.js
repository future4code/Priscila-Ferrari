import React from "react"
import {HeaderContainer, TitleHeader} from "./styled"


const Header = () => {
    return(
        <HeaderContainer>
            <TitleHeader>
                PROMOBIT
            </TitleHeader>
        </HeaderContainer>
    );
}

export default Header;